
import merge from 'lodash/merge'

const providersReducer = (state = {}, action) => {
  let nextState;
  Object.freeze(state);

  switch(action.type) {
    
    default:
      return state;
  }
};

export default providersReducer;
